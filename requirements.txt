# Dependencias del proyecto de Minería de Datos (SIAP)
# Probado con Python 3.13

pandas>=2.0
numpy>=1.24
scikit-learn>=1.6        # metadata routing (params=) usado en cross_validate/learning_curve
xgboost>=2.0             # API multi:softprob, tree_method='hist'
shap>=0.46               # shap_values multiclase — última disponible: 0.49.x
optuna>=3.0              # búsqueda bayesiana de hiperparámetros (Sección 3)
matplotlib>=3.7
seaborn>=0.12
catboost>=1.2.5
fastapi>=0.100.0
uvicorn>=0.36.0
pydantic>=2.0.0


# Ejecución/render del sitio Quarto
jupyter
nbformat
nbclient
ipykernel
